// import { getUserInfo } from "@/api/opServiceAmis/companyRegister";

// export const setUserInfoAction = ({ commit, state }) => {
//   getUserInfo(state.token).then((res) => {
//     if (res.code === "200") {
//       commit("setUserInfo", res.data);
//     }
//   });
// };
